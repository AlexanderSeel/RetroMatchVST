CI verification trigger for main after the one-shot reference-lifecycle integration.
Current source fix: recursive lifecycle normalization now mutates a private VoiceParameters copy before assigning it back to the immutable shared_ptr<const VoiceParameters> rack layer.
Validate Windows and macOS native plug-in builds plus DSP tests.
