name: Apply DSP routing v2 parallel FX

on:
  push:
    branches: [main]
    paths:
      - scripts/apply-dsp-routing-v2-parallel-fx.py
      - .github/workflows/apply-dsp-routing-v2-parallel-fx.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Apply source patch
        run: python scripts/apply-dsp-routing-v2-parallel-fx.py
      - name: Diff hygiene
        run: git diff --check
      - name: Commit product changes directly to main
        run: |
          git config user.name "RetroMatch Plan Bot"
          git config user.email "actions@users.noreply.github.com"
          rm -f scripts/apply-dsp-routing-v2-parallel-fx.py .github/workflows/apply-dsp-routing-v2-parallel-fx.yml
          git add -A
          git commit -m "Add compensated parallel FX routing"
          git pull --rebase origin main
          git push origin HEAD:main
